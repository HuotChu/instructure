define(['testharness', 'request', 'db'],
    function (harness, request, db) {
        return {
            run: function () {
                "use strict";
                
                console.log('Test harness loaded successfully.')
            }
        };
    }
);
